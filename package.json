{
  "name": "abschlussprojekt-monorepo",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "build": "npm --prefix backend run build",
    "start": "npm --prefix backend run start"
  }
}
